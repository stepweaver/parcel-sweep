{
  "name": "parcel-sweep-root",
  "version": "1.0.0",
  "private": true,
  "author": "Stephen Weaver <hello@stepweaver.dev> (https://stepweaver.dev)",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w backend && npm run build -w frontend",
    "start": "npm run start -w backend",
    "start:prod": "npm run build && npm run start",
    "typecheck": "npm run typecheck -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
